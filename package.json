{
  "name": "async-request-context",
  "version": "1.0.0",
  "description": "Async storage for request context",
  "main": "dist/index.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "rm -rf dist && tsc",
    "test": "node --require ts-node/register --test --watch ./src/*.test.ts",
    "lint:fix": "npx aliksend-linter --fix"
  },
  "keywords": [
    "async",
    "context",
    "logging"
  ],
  "author": "Alik Send",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aliksend/async-request-context.git"
  },
  "publishConfig": {
    "access": "public"
  }
}
